-scrpt pattern_matching

name <- 'Michael'
name <- 'Michael'
name <- 'Seva'
name <- @michael

; Check this problems
; date <- '
; @good <- @cs:outl()

another <- @MICHAEL

msg <- @hello
msg <- name
msg <- @hi

; cs:outl() <- @bad

cs:outl('msg = ', msg)
cs:outl('name = ', name)
cs:outl('another = ', another)
